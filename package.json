{
  "name": "test",
  "version": "1.0.0",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "packager": "electron-packager ./ --win --out test -arch=x64 --version 1.4.0 --overwrite --icon=./icon/icon.ico"
  },
  "devDependencies": {
  	"electron": "~1.7.8",
    "electron-packager": "^10.1.0"
  }
}
